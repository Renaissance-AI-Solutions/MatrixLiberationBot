# ============================================================
# Liberation Bot — Python Dependencies (Phase II)
# ============================================================

# --- Matrix Client ---
matrix-nio[e2e]==0.25.2
simplematrixbotlib==2.6.3
aiohttp==3.11.12

# --- Database ---
aiosqlite==0.21.0

# --- Encryption (Dead Man's Switch vault) ---
cryptography==44.0.1

# --- Scheduler ---
apscheduler==3.11.0

# --- Environment ---
python-dotenv==1.0.1
aiofiles==24.1.0

# --- Agentic AI Core (Kimi K2 via NVIDIA NIM) ---
# OpenAI-compatible Python client for NVIDIA NIM API
openai>=1.35.0

# --- Liberation Archives (NotebookLM) ---
# Unofficial Python API for Google NotebookLM
# Repo: https://github.com/teng-lin/notebooklm-py
notebooklm-py>=0.3.3

# --- Web Search (Tiered: DDG → Serper → Tavily) ---
# Tier 1: DuckDuckGo (free, no API key required)
duckduckgo-search>=6.3.7

# Tier 2: Serper.dev (optional, requires SERPER_API_KEY)
# requests is already a transitive dependency of many packages above;
# listed explicitly for clarity.
requests>=2.32.0

# Tier 3: Tavily (optional, requires TAVILY_API_KEY)
tavily-python>=0.5.0

# --- Testing ---
pytest==8.3.4
pytest-asyncio==0.25.3
